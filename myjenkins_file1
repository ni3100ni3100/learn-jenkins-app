pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh '''
                    ls -la
                    pwd
                    touch demo1.txt
                    ls -la
                '''
            }
        }
    }
}
